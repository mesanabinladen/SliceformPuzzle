Pyinstaller instructions

Move on the project folder and execute the following:

on Windows 

pyinstaller --onefile --noconsole --name "SliceformMaker" main.py  

on MAC

pyinstaller --windowed --name "SliceformMaker" main.py

